import { Meteor } from 'meteor/meteor';
//below.. this has to be imported here so meteor will load this file on the server side
import '../imports/api/ContactsCollection';


Meteor.startup(() => {
});
